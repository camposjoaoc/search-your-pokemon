[build]
  command = "npm run build"
  publish = "dist"

# Serve index.html for every route so React Router handles client-side navigation
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
